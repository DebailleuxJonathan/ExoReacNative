import {StyleSheet} from 'react-native';

const stylesTodoListDetails = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'column',
    margin: 10,
    flex: 1,
  },
});

export default stylesTodoListDetails;
